// Este arquivo pode ser removido ou substituído por um `.eslintrc.cjs` mais tradicional.
// Por enquanto, vamos deixá-lo vazio para não interferir,
// já que o `package.json` agora aponta para uma configuração mais padrão.
// O Vite e o plugin do React já fornecem uma boa base de linting.
export default [];
